<%= form_with(model: agenda, local: true) do |form| %>
  <% if agenda.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(agenda.errors.count, "error") %> prohibited this agenda from being saved:</h2>

      <ul>
      <% agenda.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :agenda_item %>
    <%= form.text_field :agenda_item, id: :agenda_agenda_item %>
  </div>

  <div class="field">
    <%= form.label :is_complete %>
    <%= form.check_box :is_complete, id: :agenda_is_complete %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
